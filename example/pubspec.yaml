name: flutter_bluetooth_plus_example
description: Demonstrates how to use the flutter_bluetooth_plus plugin.
publish_to: 'none'

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.19.5"

dependencies:
  flutter:
    sdk: flutter
  path_provider: 2.1.5 #^1.4.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_bluetooth_plus:
    path: ../

flutter:
  uses-material-design: true
